# easymail
EasyMail is an open-source mail system built with Go, integrating an SMTP gateway (Milter), mail delivery (LMTP), IMAP4 server, Dovecot authentication, Webmail frontend, admin panel, and a built-in anti-spam AI engine (supporting FastText / DistilBERT / XGBoost multi-algorithm classifiers).
